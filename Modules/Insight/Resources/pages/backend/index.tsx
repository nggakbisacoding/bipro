import { Head } from "@inertiajs/react";
import { PageProps } from "@/types";

export default function Insight({ auth }: PageProps) {
    return (
        <>
            <Head title="Insight" />

            <span>Insight Admin</span>
        </>
    );
}
